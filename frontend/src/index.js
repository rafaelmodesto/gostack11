import React from 'react';
import { render } from 'react-dom';

//importação de um componente
import App from './App';

//Chamamos o componente como uma tag HTML.
render(<App />, document.getElementById('app'));